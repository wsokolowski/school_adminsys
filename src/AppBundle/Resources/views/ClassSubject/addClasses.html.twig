{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Add class subject</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('show_my_courses') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}